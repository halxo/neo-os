---
groupsUrl: local
required: true

label:
    sidebar: "Package Source"
    title: "Choose where NeoOS gets its system packages"

groups:
    - name: "Arch Linux"
      description: "Use standard Arch Linux packages. Recommended for maximum compatibility."
      selected: true
      critical: true
      packages:
        - neo-source-arch

    - name: "CachyOS Optimized"
      description: "Use optimized CachyOS packages when available for your CPU."
      selected: false
      critical: true
      packages:
        - neo-source-cachyos
